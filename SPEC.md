# FF14 絶妖星乱舞 P4 カンペアプリ 仕様書

更新: 2026-09-03

## 1. 目的

FF14「絶妖星乱舞」P4で、GC1/GC2中に確認した真偽・個人デバフ・カオスの炎/水を短時間で入力し、後半の処理内容を横画面のiPhone/iPad上にカンペとして表示する。

## 2. 配布・実行方式

- GitHub Pagesで公開する静的Webアプリ
- サーバー、DB、ログイン、APIは使用しない
- HTML / CSS / JavaScriptのみで構成する
- 設定値のみ `localStorage` に保存する
- Cookieは使用しない
- GC入力内容・マジックアウト状態は再読み込み後に復元しない

## 3. 共通ヘッダー

GC1 / GC2 / 処理内容のすべてで同じ高さ・同じ配置のヘッダーを使用する。

| 位置 | 内容 |
|---|---|
| 左 | リセット |
| 中央 | GC1 / GC2 / 処理内容、GC画面のみカウントダウン |
| 右 | `操作なし: X秒`、設定ボタン |

処理内容だけメインコンテンツが上へ詰まらないよう、画面ごとに別ヘッダーを持たず共通ヘッダーを1つ使用する。

## 4. 画面遷移と配色

1. GC1入力
2. GC2入力
3. 処理内容
4. リセットでGC1へ戻る

配色ルール：

- 奇数側（GC1 / 処理内容）：既存の黄系アクセント
- 偶数側（GC2）：少し青みのあるアクセント

GC2への切り替わりを視覚的に把握できる程度の差に留め、レイアウト・ボタン配置は変更しない。

## 5. GCタイマー

- GC1既定値：10秒
- GC2既定値：10秒
- 各GCで最初の手動入力時に開始
- GC1から自動補完されたGC2項目だけではGC2タイマーを開始しない
- 時間経過または完了ボタンで次画面へ進む
- 設定可能範囲：各1～60秒
- 進行中に設定値を変更しても現在の締切は変更せず、次回開始から反映

## 6. GC2自動補完

- GC1炎 → GC2水
- GC1水 → GC2炎
- GC1雷/水 → GC2加速度を自動ON
- GC1加速度 → GC2加速度を選択不可、雷/水を手動選択
- カオス真偽とエクスデス真偽はGC2でも手動入力

## 7. 未操作時間表示と自動リセット

### 7.1 表示

右上に `操作なし: X秒` を1秒単位で表示する。

ユーザー操作として扱うもの：

- pointerdown（タップ / クリック）
- キー操作

内部タイマー更新や自動畫面切替は操作時刻を更新しない。

### 7.2 自動リセット

既定値：

- 有効
- 300秒

設定可能範囲：30～3600秒。

時間到達時に以下をリセットする。

- GC1 / GC2入力
- GC2自動補完状態
- GCタイマー
- マジックアウト記録

設定値そのものは保持する。

## 8. 処理内容

### 8.1 個人デバフ

GC1 / GC2で入力した個人デバフと処理内容を表示する。

### 8.2 固定タイムライン

1. 視線1
2. 炎
3. 視線2
4. 水

### 8.3 マジックアウト

`直線` / `扇` は既定で `踏まない`。

| 直線 | 扇 | 真 | 偽 |
|---|---|---|---|
| 踏まない | 踏まない | 全部踏まない | 全部踏む |
| 踏む | 踏まない | 直線踏む | 扇踏む |
| 踏まない | 踏む | 扇踏む | 直線踏む |
| 踏む | 踏む | 全部踏む | 全部踏まない |

操作ボタンと結果表示カードを同色にしない。`直線` / `扇` は専用の緑～シアン系操作色を使用する。

真・偽のラベル列幅は共通化し、結果文言の開始位置を揃える。

## 9. 設定画面

右上の歯車ボタンでオーバーレイ表示する。

設定項目：

- GC1 自動切替秒数
- GC2 自動切替秒数
- 未操作自動リセット ON/OFF
- 未操作リセット秒数

操作：

- 保存
- 既定値に戻す
- × または背景タップで閉じる

`既定値に戻す` はフォーム上の値を既定値へ戻し、`保存` 時に確定する。

## 10. localStorage

キー：

```text
ff14P4Cheatsheet.settings.v1
```

保存データ：

```json
{
  "gc1Seconds": 10,
  "gc2Seconds": 10,
  "idleResetEnabled": true,
  "idleResetSeconds": 300
}
```

読み込み失敗・JSON破損・localStorage利用不可時は既定値で動作する。

## 11. バージョン情報

設定画面最下部に表示する。

```text
Version 2026.09.03-r1
Updated 2026-09-03 15:17 JST
```

JavaScriptの `APP_INFO` を表示元とする。

## 12. レスポンシブ・誤操作対策

- 横画面を主対象
- `100vw × 100dvh`
- Safe Area考慮
- 低高さ画面ではヘッダー、結果カード、マジックアウト領域を段階的に圧縮
- 縦画面では横向き案内を表示
- 文字選択や不要なタッチ挙動を抑制
- 主要ボタンのタップ領域を確保する

## 13. 著作権表記

画面下部に `FINAL FANTASY XIV © SQUARE ENIX` を常時表示する。
