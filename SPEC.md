# FF14 絶妖星乱舞 P4 カンペアプリ 仕様書

## 1. 目的

FF14「絶妖星乱舞」P4で、GC1/GC2中に確認した真偽・個人デバフ・カオスの炎/水を短時間で入力し、後半の処理内容を横画面のiPhone/iPad上にカンペとして表示する。

本アプリは攻略タイムラインそのものを再現するものではなく、プレイヤーがゲーム画面で確認した情報を記録し、後半に必要な処理へ変換して表示することを目的とする。

## 2. 配布・実行方式

- GitHub Pagesで公開する静的Webアプリ
- サーバー、DB、ログイン、APIは使用しない
- HTML / CSS / JavaScriptのみで構成する
- iPhone / iPadの横画面を主対象とする
- ページ再読み込み時は初期状態に戻す

## 3. 対象デバイス・表示

- iPhone 横画面
- iPad 横画面
- 画面サイズは固定せず、`100vw × 100dvh`を基準にレスポンシブ対応
- Safe Area（ノッチ / Dynamic Island等）を考慮
- 縦画面では「横向きで使用してください」を表示
- 画面スクロールは原則発生させない
- ダブルタップ拡大や文字選択など、戦闘中の誤操作につながる挙動を抑制

## 4. 画面遷移

1. GC1入力画面
2. GC2入力画面
3. 処理内容画面
4. リセットでGC1入力画面へ戻る

### 4.1 タイマー

- GC1 / GC2ともに10秒
- 各フェーズで最初の入力ボタンを押した瞬間にカウント開始
- フェーズ表示直後にはカウントを開始しない
- 表示文言：`あと X秒`
- 10秒経過時に自動確定して次画面へ進む
- GC1終了後、GC2に遷移してもGC2最初の手動入力まではタイマー停止
- GC1の内容からGC2へ自動補完された項目は、GC2タイマー開始のトリガーにしない

### 4.2 手動確定

- GC1：`GC1完了`
- GC2：`GC2完了`
- 最初の入力後に有効化
- 押下時は残り時間を待たず即時確定
- 確認ダイアログは表示しない

## 5. GC入力画面

画面を4象限に分割する。

| 位置 | 内容 |
|---|---|
| 左上 | カオス 真偽 |
| 左下 | カオス 炎 / 水 |
| 右上 | エクスデス 真偽 |
| 右下 | エクスデス 雷 / 水 / 加速度 |

画面上中央に `GC1` / `GC2` とカウントダウンを表示し、画面下中央に完了ボタンを配置する。

### 5.1 真偽

- カオス：真 / 偽の排他選択
- エクスデス：真 / 偽の排他選択
- 将来的に真偽それぞれを画像アイコンへ差し替える
- 選択済みボタンは明確に強調表示
- 選択済みボタンを再度タップすると解除可能

### 5.2 カオス 炎 / 水

- GC1では炎 / 水の排他選択
- 画像アイコンへ差し替え可能
- 同じボタン再タップで解除可能
- 炎 / 水はGC1・GC2で必ず片方ずつ出現するため、GC1で選択済みの場合はGC2を反対側へ自動補完する
  - GC1＝炎 → GC2＝水
  - GC1＝水 → GC2＝炎
- GC2で自動補完された炎 / 水は「自動」と表示し、変更不可とする
- GC1で未入力だった場合のみ、GC2では炎 / 水を通常どおり選択可能とする

### 5.3 エクスデス 個人デバフ

- 雷 / 水 / 加速度の排他選択
- 視線専用ボタンは設けない
- 画像アイコンへ差し替え可能
- 同じボタン再タップで解除可能
- デバフは各GCで「水または雷」か「加速度系」の二択で、GC1とGC2では反対系統になる
- GC1が雷または水の場合、GC2は加速度へ自動補完し、GC2の個人デバフ3ボタンは変更不可とする
- GC1が加速度の場合、GC2は雷 / 水のどちらかになるため、GC2では加速度だけを無効化して雷 / 水の2択を残す
- GC1で未入力だった場合はGC2の3択を制限しない
- 未選択状態も保持可能。結果画面では必要に応じて`未入力`として扱う

### 5.4 GC2自動補完の対象外

以下はGC1から一意に決まらないため、GC2でも必ず手動入力する。

- カオス真偽
- エクスデス真偽
- GC1が加速度だった場合のGC2の雷 / 水の別

自動補完は入力回数削減のために行うが、真偽判定そのものは各GCでゲーム画面を確認する。

## 6. 入力データ

GC1 / GC2ごとに次を保持する。

- エクスデス真偽：true / false / null
- エクスデス個人デバフ：thunder / water / acceleration / null
- カオス真偽：true / false / null
- カオス種類：fire / water / null

早/遅はアプリでは保持・判定しない。実際のゲーム画面上のデバフ残り時間をプレイヤーが確認する。

GC2の一部項目はGC1の入力値から自動生成される。自動生成値も通常入力と同じ形式でGC2レコードへ保持する。

## 7. 判定ロジック

### 7.1 エクスデス 視線

| 対象 | 真偽 | 表示 |
|---|---|---|
| 視線1（GC1） | 真 | 見ない |
| 視線1（GC1） | 偽 | 見る！ |
| 視線2（GC2） | 真 | 見ない |
| 視線2（GC2） | 偽 | 見る！ |

GC1の視線は1回目、GC2の視線は2回目として扱う。

### 7.2 エクスデス 加速度

| 加速度が付いたGCの真偽 | 表示 |
|---|---|
| 真 | 止まる |
| 偽 | 動く |

### 7.3 エクスデス 雷 / 水

| デバフ | 真偽 | 表示 |
|---|---|---|
| 雷 | 真 | 散開 |
| 雷 | 偽 | 頭割り |
| 水 | 真 | 頭割り |
| 水 | 偽 | 散開 |

早/遅は判定しない。

### 7.4 カオス 炎

| 真偽 | 表示 |
|---|---|
| 真 | タケノコ |
| 偽 | 中央 |

### 7.5 カオス 水

| 真偽 | 表示 |
|---|---|
| 真 | 中央 |
| 偽 | タケノコ |

炎 / 水がGC1・GC2のどちらで入力されたかにかかわらず、結果画面では処理順に並べ替える。

## 8. 処理内容画面

タイトル：`処理内容`

画面を左右2分割する。基本比率は左40% / 右60%。

### 8.1 左側：個人デバフの処理

GC1 / GC2で入力された個人デバフについて、アイコンと処理内容を大きく表示する。

例：

- 雷アイコン + `散開`
- 加速度アイコン + `止まる`

早/遅の順序はアプリでは決定しない。

### 8.2 右側：固定タイムライン

以下の順番で縦タイムライン表示する。

1. 視線1
2. 炎
3. 視線2
4. 水

各項目は「アイコン / 項目名」と処理内容を表示する。

例：

1. 視線1 → `見る！`
2. 炎 → `タケノコ`
3. 視線2 → `見ない`
4. 水 → `中央`

項目間は矢印等で順序を視覚的に示す。

## 9. 表示文言

| 用途 | 表示 |
|---|---|
| フェーズ | GC1 / GC2 |
| GC1手動確定 | GC1完了 |
| GC2手動確定 | GC2完了 |
| カウントダウン | あと X秒 |
| カウント開始前 | 入力待ち |
| 最終画面タイトル | 処理内容 |
| 視線・見る | 見る！ |
| 視線・見ない | 見ない |
| 雷水・散開 | 散開 |
| 雷水・頭割り | 頭割り |
| 加速度・停止 | 止まる |
| 加速度・移動 | 動く |
| カオス炎水・中央 | 中央 |
| カオス炎水・タケノコ | タケノコ |
| 初期化 | リセット |
| 入力不足 | 未入力 |

## 10. 画像差し替え

画像は`assets/icons/`配下に配置する。

想定ファイル名：

- `chaos-true.png`
- `chaos-false.png`
- `chaos-fire.png`
- `chaos-water.png`
- `exdeath-true.png`
- `exdeath-false.png`
- `exdeath-thunder.png`
- `exdeath-water.png`
- `exdeath-acceleration.png`
- `gaze.png`

画像が存在しない場合は、テキストのフォールバック表示を使用する。

## 11. UX方針

- 実戦中の視認性を優先
- タップ領域を大きくする
- 選択状態を強く表示する
- 確認ダイアログは極力使用しない
- 入力画面は4象限の位置をGC1/GC2で変えない
- GC2で自動補完されたボタンは選択状態を維持しつつ「自動」と表示する
- GC1の情報から不可能と確定したGC2候補は押せない状態にする
- 最終画面は「左＝個人処理」「右＝固定タイムライン」を維持
- タイマーは補助情報として表示し、入力ボタンより主張を弱くする

## 12. 初期設定値

- GC1自動確定：10秒
- GC2自動確定：10秒

録画済み動画等でテスト後、JavaScript内の設定値を変更して調整可能とする。
